<p><%= @user.name %></p>
<p><%= @user.email %></p>

<%= link_to '編集', edit_user_path(@user), class: 'btn btn-primary' %>
<%= button_to '削除', user_path(@user), method: :delete, form: { data: { turbo_confirm: "本当に削除しますか？" }}, class: 'btn btn-danger' %>